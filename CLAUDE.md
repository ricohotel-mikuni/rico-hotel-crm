# 大栄商事株式会社 統合管理システム — 開発方針

## HotelOS Development Specification(最上位のビジョン文書)

- **[HotelOS Development Specification v1.0](./docs/hotelos-development-specification.md)** — ホテル事業モジュールが目指す姿を定める開発方針名「HotelOS」の定義(フロントシステムではなく、ホテル経営をAIが支援するOperating System)・最終目標(日本一利益を生み出すホテル経営システム)・展開方針(リコホテル三国での実運用→複数ホテル/ホテルグループ→将来的なSaaS化を前提とした設計原則)を規定する。**「HotelOS」は社内の開発方針名であり、画面上のブランド表示ではない** — ユーザーに見える名称・ロゴは下記のブランドロゴ規定のとおり大栄商事株式会社／各ホテルの正式名称・ロゴを維持する。

## 開発憲章(正式な基準文書)

本システムの開発は、以下の開発憲章を最上位の基準として行う。承認済み・発効済み。このファイル(`CLAUDE.md`)は運用上の要約・索引であり、内容が食い違う場合は憲章本文を正とする。

- **[大栄商事ERP開発憲章 Ver.1.3](./docs/development-constitution.md)** — システム設計・UI/UX・ブランド・認証/ログイン・権限・DB/マイグレーション・開発フロー・拡張性・UIデザインシステムの全原則
- **[大栄商事AI開発憲章 Ver.1.2](./docs/ai-development-constitution.md)** — AIレベル(Level1〜6)・AI Copilot・AI Today・AIアシスタント「DAI」の人格/応答方針・AIの権限境界・提案評価原則・データ利用/運用ルール(ERP憲章の下位文書)
- **[大栄商事株式会社 UIデザインシステム v1.0](./docs/ui-design-system.md)** — 配色トークン・カードパターン(○○ TODAY・KPIグリッド・二分割パネル等)・サイドバー/ヘッダー/NEOチャットの共通UI規定(ERP憲章第十一章に基づく下位文書)。新規画面は原則この文書のパターンに従う。既存画面への適用は画面ごとの個別提案を経て段階的に行う(ERP憲章第37条)。

新機能・画面変更を検討する際は、必ずこれらの文書に矛盾しないことを確認する。矛盾する変更が必要な場合は、機能提案の前に憲章改定(各憲章の改定条項)を提起する。AI機能(AI Copilot・AI Today・分析・提案系)を扱う場合は、AI開発憲章の**AIレベル定義(第二章)**と**権限の一覧(第23条)**を必ず確認し、提案書に該当レベルを明記する。ホテル事業モジュールに関する新機能・データ設計は、HotelOS Development Specificationの展開方針(複数ホテル/SaaS前提)にも矛盾しないことを確認する。

## 最重要ルール: 実装前に必ず提案書を作成し、承認を得る

以下に該当する変更は、着手前に必ず提案書を作成し提示すること: 画面変更・デザイン変更・UI変更・UX変更・画面遷移変更・ロゴ変更・アニメーション追加・カラー変更・新機能追加・ダッシュボード追加。

提案書には以下を含める:

1. 現在の画面
2. 改善案
3. ワイヤーフレーム
4. デザインモックアップ（HTMLプレビュー／Artifact、実際に動作する形が望ましい）
5. 画面遷移図
6. 変更点一覧
7. メリット・デメリット
8. 実装範囲

ユーザーが明示的に **「承認します」**・**「実装してください」**・**「実装OK」** のいずれかと言うまで、コード変更・データベース変更・画面変更・デザイン変更・実装を一切開始しない。

小さな修正であっても省略しない。例外はユーザーが明示的に緊急のバグ修正等を指示した場合のみ（その場合も事後に変更内容を報告する）。

## システム設計方針

大栄商事株式会社を頂点とするERPシステムとして設計する。単なるホテル管理システムではない。

```
大栄商事株式会社
└── 事業
    ├── ホテル事業 → 各ホテル（現在: リコホテル三国）
    └── 賃貸事業 → 各物件（未着手）
```

この3階層構成を維持する。中間画面・ホーム画面を不必要に増やさない。

## ナビゲーション規則（全画面共通）

- 左上に「← 1つ前へ戻る」ボタンを設置する。ブラウザの戻るではなく、システム内のロジカルな親画面へ戻る仕様とする。
- パンくずリスト（例: 🏢大栄商事 ＞ 🏨ホテル事業 ＞ リコホテル三国 ＞ 営業管理）を全画面に実装する。各階層はクリックでその画面へ遷移する。
- 戻るボタンの名称・デザイン・サイズ・配置は全システムで統一する。

## 管理センター（管理者専用・一般スタッフには非表示）

銀行・融資管理／書類管理（全社横断）／システム管理／システムアーカイブ／権限管理／監査ログ／バックアップ を集約する。

## 業務フロー・将来拡張（設計時に必ず考慮する）

- **営業**: 営業先 → 案件 → 見積書 → 提案書 → 契約 → 請求書 → 売掛金管理（将来） → 入金管理 → 売上分析。将来的に請求書→入金の自動連携まで拡張できる構成にする。
- **ホテル運営**: フロント／清掃／朝食／夕食／設備／シフト。朝食・夕食は将来 メニュー／在庫／発注／原価／売上 まで拡張できる設計にする。
- **書類管理**: 営業書類（見積書・提案書・契約書・請求書・領収書）とフロント書類（請求書・領収書・宿泊証明書・利用明細）は一覧・管理を分離する。管理者のみ全社横断検索・一覧が可能。
- **帳票共通機能**（全書類に標準で必要）: A4対応・PDF生成・保存・印刷・印刷前PDFプレビュー・メール送信・再発行・発行履歴・インボイス制度対応。
- **将来拡張予定**（追加しやすい構造で設計する）: 在庫管理／電子署名／電子印影／テンプレート管理／メール送信履歴／AI文章作成／AI売上分析／AI営業分析。

## 認証・ログイン規定(要約 — 詳細はERP開発憲章第五章)

パスワード認証(Supabase Auth)を基盤とし、PINは「生きたままのSupabaseセッションの上に被せるロック画面」を解除するゲートであって、セッションの破棄・復元は一切行わない(`AuthContext`の`lock()`/`unlock()`、Supabaseへは非アクセス)。PIN等の秘匿情報はハッシュもクライアントへ返さず、照合は`SECURITY DEFINER`関数(Edge Function/service-role基盤が無いため)を唯一の入口とする。簡易認証は5回誤りで30秒ロック、ロック2回連続でパスワード認証に差し戻す。信頼済み端末は最終利用から30日のスライディング有効期限(使うたびに延長)または管理者失効でのみ無効化する。**重要な既知の落とし穴**: `supabase.auth.signOut()`は`scope`の指定に関わらず「今使っているこのセッション」自身のrefresh tokenを必ず失効させる(`scope:'local'`は他のセッションを巻き込まないという意味でしかない) — そのため「ログアウトしたセッションのトークンを後で再提示する」設計は原理的に成立しない(2026-07-09に実装・修正済み)。認証の成功・失敗はすべてログイン履歴に記録し管理者が確認できる状態を保つ。この数値基準・設計原則を変更する場合は個別実装ではなく憲章改定で行う。

## AI開発原則(要約 — 詳細はAI開発憲章)

**AIは提案する。人が決定する。** 公式AIアシスタントは「DAI」と名乗り、全社共通の人格として全画面に一貫して登場する。標準口調は上品な関西弁(丁寧・柔らかい・ホテル品質、きつい/怖い/乱暴な表現は禁止)。AI機能はAIレベル(Level1: 検索・要約 〜 Level6: 条件付き自動実行)のいずれかに位置づけて設計し、現行運用はLevel1〜4まで(Level5・6は個別の憲章改定なしに実装しない)。送金・契約確定・請求書発行・給与変更・パスワード/PIN変更・電子承認の承認行為は、AIがどのレベルであっても常に人間の実行を必須とする。分析・提案は「利益・売上・業務効率・顧客満足度」を優先評価軸とするが、法令遵守・公平性・顧客への誠実な対応には優先させない。AIが利用不可でもコア業務は全機能すること(縮退動作必須)。

## ブランドロゴの取り扱い（厳守）

正式ロゴ（Aマーク＋大栄商事株式会社＋DAIEI SHOJI CO., LTD.＋左右の金色ライン、リコホテル三国は同様にアーチ＋Rマーク＋RICO HOTEL＋ー MIKUNI ー）は、文字・英字・金色ラインを**絶対に削除・トリミングしない**。以前クロップされたA マーク単体版（`public/brand-daiei-icon.png`）を汎用ロゴとして使い回していたことが問題として指摘された — 同じ後退を繰り返さないこと。

例外的にAマーク/Rマーク単体（アイコンのみ）を使ってよいのは: iPhone/iPadホーム画面アイコン・favicon・通知アイコン・小さいボタンのみ。それ以外の画面（ログイン画面・会社ホーム・ホテルホーム・サイドバー・ヘッダー・各画面）は必ず正式ロゴ（全要素込み）を使用する。ロゴの比率も変更しない。

## 最終目標

ホテル・賃貸・営業・社員・電子承認・書類・銀行・経営・AIを一元管理する、大栄商事全体のERPシステムとして設計・実装する。
